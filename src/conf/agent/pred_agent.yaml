memory:
  type: "episode"
  params:
    max_size: ${mul:${task.num_envs}, ${run.iterations}}

steps_per_iteration: ${task.steps_per_iteration}
steps_per_evaluation: ${task.steps_per_evaluation}
reset_memory: false